import React from 'react';

const Login = () => {
    return (
        <form>
            <input type="text"></input>
            <input type="password"></input>
            <button>Login</button>
        </form>
    );
};

export default Login;

